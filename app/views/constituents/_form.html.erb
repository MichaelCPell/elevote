<%= form_for(@constituent) do |f| %>
  <% if @constituent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@constituent.errors.count, "error") %> prohibited this constituent from being saved:</h2>

      <ul>
      <% @constituent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
