<%= form_for(@precinct) do |f| %>
  <% if @precinct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@precinct.errors.count, "error") %> prohibited this precinct from being saved:</h2>

      <ul>
      <% @precinct.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
